<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.zhengsigen.java.aop.dao.UserService">

<!-- 	<select id="getTypeByUser"
		resultType="cn.zhengsigen.java.aop.pojo.User">
		select id,type from user where name=#{name} and
		passwd=#{passwd}
	</select>
	<select id="getcountFromUser" resultType="integer">
		select count(1) from
		user where name=#{name}
	</select>
	<insert id="addUser">
		INSERT INTO user
		(name, passwd)
		VALUES (#{name},#{passwd})
	</insert>
	 -->
	<insert id="addUser">
		INSERT INTO user
		(name)
		VALUE (#{name})
	</insert>
</mapper>
